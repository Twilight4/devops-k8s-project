* Project overview (To-do list)
- 3 microservices (small, independent):
  + *api* — front-end API (Node.js / Express or Flask)
  + *users* — user service (CRUD, REST)
  + *orders* — order service (CRUD, REST)
- *Database*: PostgreSQL (one central DB or one per service — start with a single DB via PVC)
- *Ingress*: HTTP routing with host/path rules
- *Config*: ConfigMaps & Secrets
- *Persistence*: PersistentVolumeClaim for Postgres
- *Deployment*: Deployments + Services + Horizontal Pod Autoscaler (HPA)
- *Packaging*: Helm chart for the app
- *CI/CD*: GitHub Actions to build/push images and deploy to cluster (minikube for demo; optional EKS/GKE/AKS)

* Quick prerequisite checklist
Install (locally):
- git
- Docker (or Docker Desktop)
- kubectl
- minikube or kind (I recommend kind for reproducible CI-friendly clusters)
- helm (v3)
- (optional) kubectl port-forward or kubectl proxy for demos
  
* Repo layout
#+begin_src shell
devops-k8s-project/
├─ README.org
├─ .github/
│  └─ workflows/
│     └─ ci-cd.yml
├─ charts/                        # Helm chart root (or use helm create)
│  └─ microservices/
├─ deploy/                        # Raw manifests (for learning/demo)
│  ├─ namespace.yaml
│  ├─ frontend-deployment.yaml
│  ├─ users-deployment.yaml
│  ├─ orders-deployment.yaml
│  ├─ postgres-deployment.yaml
│  └─ ingress.yaml
├─ services/
│  ├─ users-service/
│  │  ├─ Dockerfile
│  │  ├─ src/
│  │  ├─ package-lock.json (generated)
│  │  └─ package.json
│  ├─ orders-service/
│  │  ├─ Dockerfile
│  │  ├─ src/
│  │  ├─ package-lock.json (generated)
│  │  └─ package.json
│  └─ api-gateway/
│     ├─ Dockerfile
│     ├─ src/
│     ├─ package-lock.json (generated)
│     └─ package.json
├─ infra/
│  └─ storageclass.yaml (optional)
└─ docs/
   └─ architecture.png
#+end_src

* Build and test locally minimal microservice
It is using Node.js Express service to replicate for =api=, =users=, =orders=.

#+begin_src shell
# Log in to dockerhub
docker login

# Build and run the users image
cd services/users-service/
npm install                         # Run this command to generate package-lock.json file needed for docker build command
docker build -t <YOUR_HUB_NAME>/users:0.1.0 .
docker run -p 3001:3001 <YOUR_HUB_NAME>/users:0.1.0

# test the users image
docker ps
curl http://localhost:3001/users

# Build and run the orders image
cd services/orders-service/
npm install                        # Run this command to generate package-lock.json file needed for docker build command
docker build -t <YOUR_HUB_NAME>/orders:0.1.0 .
docker run -p 3002:3002 <YOUR_HUB_NAME>/orders:0.1.0

# test the orders image
docker ps
curl http://localhost:3002/orders

# Build and run the api-gateway image
cd services/api-gateway/
npm install                        # Run this command to generate package-lock.json file needed for docker build command
docker build -t <YOUR_HUB_NAME>/api:0.1.0 .
# To check docker's IP: ip route | grep docker (default on linux is 172.17.0.1)
docker run -it -p 3000:3000 \
  -e USERS_URL=http://172.17.0.1:3001 \
  -e ORDERS_URL=http://172.17.0.1:3002 \
  twilight4/api:0.1.0

# test the api-gateway image
docker ps
curl http://localhost:3000/users
curl http://localhost:3000/orders
curl http://localhost:3000/health
#+end_src

* Kubernetes manifests (deployment + service + ingress)
